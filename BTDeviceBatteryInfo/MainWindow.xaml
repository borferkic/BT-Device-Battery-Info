<Window x:Class="BTDeviceBatteryInfo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BT Device Battery Info"
        Width="380" Height="250"
        MinWidth="380" MaxWidth="380"
        MinHeight="250"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#FF202020" AllowsTransparency="False"
        ShowInTaskbar="False">
    <Border Style="{StaticResource WidgetBorder}" MouseLeftButtonDown="DragWindow">
        <Grid Margin="18,12,18,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="BT Device Battery Info" Style="{StaticResource TitleText}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="&#xE921;" ToolTip="Minimize to tray" Style="{StaticResource IconButton}" Click="Minimize_Click"/>
                <TextBlock Grid.Column="2" Text="&#xE713;" ToolTip="Settings unavailable" Width="30" Height="30" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#FFE0E0E0" Opacity="0.45" TextAlignment="Center" VerticalAlignment="Center" Padding="0,6,0,0"/>
                <Button Grid.Column="3" Content="&#xE8BB;" ToolTip="Exit" Style="{StaticResource CloseIconButton}" Click="Exit_Click"/>
            </Grid>
            <Grid Grid.Row="1" Margin="0,16,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Your devices" Style="{StaticResource SectionTitleText}"/>
                <TextBlock Grid.Row="1" Text="{Binding DeviceListMessage}" Visibility="{Binding DeviceListMessageVisibility}" Foreground="#FFB8B8B8" FontSize="12" Margin="0,6,0,10" TextWrapping="Wrap"/>
                <ItemsControl Grid.Row="2" ItemsSource="{Binding Devices}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="Transparent" Padding="2,11" Margin="0" MinHeight="56">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" Foreground="#FFF2F2F2" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0" HorizontalAlignment="Right">
                                            <TextBlock Text="Connected" Foreground="#FF69D36F" FontSize="11" HorizontalAlignment="Right"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,3,0,0">
                                                <Grid Width="23" Height="12" Margin="0,0,6,0" VerticalAlignment="Center" ToolTip="{Binding BatteryText}">
                                                    <Border Width="19" Height="10" HorizontalAlignment="Left" VerticalAlignment="Center" BorderBrush="{Binding BatteryBrush}" BorderThickness="1" CornerRadius="2">
                                                        <Border Width="{Binding BatteryFillWidth}" Height="6" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2,0,0,0" Background="{Binding BatteryBrush}" CornerRadius="1"/>
                                                    </Border>
                                                    <Border Width="2" Height="5" HorizontalAlignment="Right" VerticalAlignment="Center" Background="{Binding BatteryBrush}" CornerRadius="0,1,1,0"/>
                                                </Grid>
                                                <TextBlock Text="{Binding BatteryText}" Foreground="{Binding BatteryBrush}" FontSize="12" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
            <Grid Grid.Row="1" Background="#FF202020" Visibility="{Binding LoadingVisibility}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Ellipse Width="38" Height="38" Stroke="#FFE0E0E0" StrokeThickness="3" StrokeDashArray="12,6" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Center">
                        <Ellipse.RenderTransform>
                            <RotateTransform/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:0.9"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    <TextBlock Text="Loading app, please wait..." Foreground="#FFF2F2F2" FontSize="12" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
            <TextBlock Grid.Row="2" Text="Development in Beta" HorizontalAlignment="Right" Foreground="#FF8A8A8A" FontSize="10" Margin="0,10,0,0"/>
        </Grid>
    </Border>
</Window>
